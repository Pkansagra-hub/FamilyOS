{
  "title": "Arbitration",
  "panels": [
    { "type": "timeseries", "title": "p95 latency", "targets":[{"expr":"histogram_quantile(0.95, sum(rate(arbitration_latency_ms_bucket[5m])) by (le))"}]},
    { "type": "timeseries", "title": "High risk count", "targets":[{"expr":"sum(increase(arbitration_high_risk_total[1h]))"}]}
  ],
  "schemaVersion": 38
}
